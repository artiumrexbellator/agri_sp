import express from "express";
const app = express();

const createWallet = () => {};

app.get("/createCommodity", (req, res) => {});

app.listen(3000, () => {
  console.log("Server started on port 3000");
});
