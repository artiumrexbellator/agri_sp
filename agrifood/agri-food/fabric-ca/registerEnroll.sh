#!/bin/bash

function createSupplier() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/supplier.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/supplier.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:7054 --caname ca-supplier --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-supplier.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-supplier.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-supplier.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-7054-ca-supplier.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/supplier.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy supplier's CA cert to supplier's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/supplier.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem" "${PWD}/organizations/peerOrganizations/supplier.com/msp/tlscacerts/ca.crt"

  # Copy supplier's CA cert to supplier's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/supplier.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem" "${PWD}/organizations/peerOrganizations/supplier.com/tlsca/tlsca.supplier.com-cert.pem"

  # Copy supplier's CA cert to supplier's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/supplier.com/ca"
  cp "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem" "${PWD}/organizations/peerOrganizations/supplier.com/ca/ca.supplier.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-supplier --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-supplier --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-supplier --id.name supplieradmin --id.secret supplieradminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:7054 --caname ca-supplier -M "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/msp" --csr.hosts peer0.supplier.com --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/supplier.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:7054 --caname ca-supplier -M "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/tls" --enrollment.profile tls --csr.hosts peer0.supplier.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/supplier.com/peers/peer0.supplier.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:7054 --caname ca-supplier -M "${PWD}/organizations/peerOrganizations/supplier.com/users/User1@supplier.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/supplier.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/supplier.com/users/User1@supplier.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://supplieradmin:supplieradminpw@localhost:7054 --caname ca-supplier -M "${PWD}/organizations/peerOrganizations/supplier.com/users/Admin@supplier.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/supplier/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/supplier.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/supplier.com/users/Admin@supplier.com/msp/config.yaml"
}

function createFarmer() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/farmer.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/farmer.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:8054 --caname ca-farmer --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-farmer.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-farmer.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-farmer.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-8054-ca-farmer.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/farmer.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy farmer's CA cert to farmer's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/farmer.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem" "${PWD}/organizations/peerOrganizations/farmer.com/msp/tlscacerts/ca.crt"

  # Copy farmer's CA cert to farmer's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/farmer.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem" "${PWD}/organizations/peerOrganizations/farmer.com/tlsca/tlsca.farmer.com-cert.pem"

  # Copy farmer's CA cert to farmer's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/farmer.com/ca"
  cp "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem" "${PWD}/organizations/peerOrganizations/farmer.com/ca/ca.farmer.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-farmer --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-farmer --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-farmer --id.name farmeradmin --id.secret farmeradminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:8054 --caname ca-farmer -M "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/msp" --csr.hosts peer0.farmer.com --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/farmer.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:8054 --caname ca-farmer -M "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/tls" --enrollment.profile tls --csr.hosts peer0.farmer.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/farmer.com/peers/peer0.farmer.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:8054 --caname ca-farmer -M "${PWD}/organizations/peerOrganizations/farmer.com/users/User1@farmer.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/farmer.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/farmer.com/users/User1@farmer.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://farmeradmin:farmeradminpw@localhost:8054 --caname ca-farmer -M "${PWD}/organizations/peerOrganizations/farmer.com/users/Admin@farmer.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/farmer/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/farmer.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/farmer.com/users/Admin@farmer.com/msp/config.yaml"
}

function createBroker() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/broker.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/broker.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:6054 --caname ca-broker --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-6054-ca-broker.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-6054-ca-broker.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-6054-ca-broker.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-6054-ca-broker.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/broker.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy broker's CA cert to broker's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/broker.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/broker/ca-cert.pem" "${PWD}/organizations/peerOrganizations/broker.com/msp/tlscacerts/ca.crt"

  # Copy broker's CA cert to broker's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/broker.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/broker/ca-cert.pem" "${PWD}/organizations/peerOrganizations/broker.com/tlsca/tlsca.broker.com-cert.pem"

  # Copy broker's CA cert to broker's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/broker.com/ca"
  cp "${PWD}/organizations/fabric-ca/broker/ca-cert.pem" "${PWD}/organizations/peerOrganizations/broker.com/ca/ca.broker.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-broker --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-broker --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-broker --id.name brokeradmin --id.secret brokeradminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:6054 --caname ca-broker -M "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/msp" --csr.hosts peer0.broker.com --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/broker.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:6054 --caname ca-broker -M "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/tls" --enrollment.profile tls --csr.hosts peer0.broker.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/broker.com/peers/peer0.broker.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:6054 --caname ca-broker -M "${PWD}/organizations/peerOrganizations/broker.com/users/User1@broker.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/broker.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/broker.com/users/User1@broker.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://brokeradmin:brokeradminpw@localhost:6054 --caname ca-broker -M "${PWD}/organizations/peerOrganizations/broker.com/users/Admin@broker.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/broker/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/broker.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/broker.com/users/Admin@broker.com/msp/config.yaml"
}

function createFactory() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/factory.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/factory.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:5054 --caname ca-factory --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-5054-ca-factory.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-5054-ca-factory.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-5054-ca-factory.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-5054-ca-factory.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/factory.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy factory's CA cert to factory's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/factory.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/factory/ca-cert.pem" "${PWD}/organizations/peerOrganizations/factory.com/msp/tlscacerts/ca.crt"

  # Copy factory's CA cert to factory's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/factory.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/factory/ca-cert.pem" "${PWD}/organizations/peerOrganizations/factory.com/tlsca/tlsca.factory.com-cert.pem"

  # Copy factory's CA cert to factory's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/factory.com/ca"
  cp "${PWD}/organizations/fabric-ca/factory/ca-cert.pem" "${PWD}/organizations/peerOrganizations/factory.com/ca/ca.factory.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-factory --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-factory --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-factory --id.name factoryadmin --id.secret factoryadminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:5054 --caname ca-factory -M "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/msp" --csr.hosts peer0.factory.com --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/factory.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:5054 --caname ca-factory -M "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/tls" --enrollment.profile tls --csr.hosts peer0.factory.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/factory.com/peers/peer0.factory.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:5054 --caname ca-factory -M "${PWD}/organizations/peerOrganizations/factory.com/users/User1@factory.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/factory.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/factory.com/users/User1@factory.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://factoryadmin:factoryadminpw@localhost:5054 --caname ca-factory -M "${PWD}/organizations/peerOrganizations/factory.com/users/Admin@factory.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/factory/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/factory.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/factory.com/users/Admin@factory.com/msp/config.yaml"
}

function createDistributor() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/distributor.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/distributor.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:4054 --caname ca-distributor --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-4054-ca-distributor.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-4054-ca-distributor.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-4054-ca-distributor.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-4054-ca-distributor.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/distributor.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy distributor's CA cert to distributor's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/distributor.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem" "${PWD}/organizations/peerOrganizations/distributor.com/msp/tlscacerts/ca.crt"

  # Copy distributor's CA cert to distributor's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/distributor.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem" "${PWD}/organizations/peerOrganizations/distributor.com/tlsca/tlsca.distributor.com-cert.pem"

  # Copy distributor's CA cert to distributor's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/distributor.com/ca"
  cp "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem" "${PWD}/organizations/peerOrganizations/distributor.com/ca/ca.distributor.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-distributor --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-distributor --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-distributor --id.name distributoradmin --id.secret distributoradminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:4054 --caname ca-distributor -M "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/msp" --csr.hosts peer0.distributor.com --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/distributor.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:4054 --caname ca-distributor -M "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/tls" --enrollment.profile tls --csr.hosts peer0.distributor.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/distributor.com/peers/peer0.distributor.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:4054 --caname ca-distributor -M "${PWD}/organizations/peerOrganizations/distributor.com/users/User1@distributor.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/distributor.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/distributor.com/users/User1@distributor.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://distributoradmin:distributoradminpw@localhost:4054 --caname ca-distributor -M "${PWD}/organizations/peerOrganizations/distributor.com/users/Admin@distributor.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/distributor/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/distributor.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/distributor.com/users/Admin@distributor.com/msp/config.yaml"
}

function createWholesaler() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/wholesaler.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/wholesaler.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:3054 --caname ca-wholesaler --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-3054-ca-wholesaler.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-3054-ca-wholesaler.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-3054-ca-wholesaler.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-3054-ca-wholesaler.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/wholesaler.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy wholesaler's CA cert to wholesaler's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/wholesaler.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem" "${PWD}/organizations/peerOrganizations/wholesaler.com/msp/tlscacerts/ca.crt"

  # Copy wholesaler's CA cert to wholesaler's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/wholesaler.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem" "${PWD}/organizations/peerOrganizations/wholesaler.com/tlsca/tlsca.wholesaler.com-cert.pem"

  # Copy wholesaler's CA cert to wholesaler's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/wholesaler.com/ca"
  cp "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem" "${PWD}/organizations/peerOrganizations/wholesaler.com/ca/ca.wholesaler.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-wholesaler --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-wholesaler --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-wholesaler --id.name wholesaleradmin --id.secret wholesaleradminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:3054 --caname ca-wholesaler -M "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/msp" --csr.hosts peer0.wholesaler.com --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/wholesaler.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:3054 --caname ca-wholesaler -M "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/tls" --enrollment.profile tls --csr.hosts peer0.wholesaler.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/wholesaler.com/peers/peer0.wholesaler.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:3054 --caname ca-wholesaler -M "${PWD}/organizations/peerOrganizations/wholesaler.com/users/User1@wholesaler.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/wholesaler.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/wholesaler.com/users/User1@wholesaler.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://wholesaleradmin:wholesaleradminpw@localhost:3054 --caname ca-wholesaler -M "${PWD}/organizations/peerOrganizations/wholesaler.com/users/Admin@wholesaler.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/wholesaler/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/wholesaler.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/wholesaler.com/users/Admin@wholesaler.com/msp/config.yaml"
}

function createRetailer() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/peerOrganizations/retailer.com/

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/peerOrganizations/retailer.com/

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:2054 --caname ca-retailer --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-2054-ca-retailer.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-2054-ca-retailer.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-2054-ca-retailer.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-2054-ca-retailer.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/peerOrganizations/retailer.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy retailer's CA cert to retailer's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/peerOrganizations/retailer.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem" "${PWD}/organizations/peerOrganizations/retailer.com/msp/tlscacerts/ca.crt"

  # Copy retailer's CA cert to retailer's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/retailer.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem" "${PWD}/organizations/peerOrganizations/retailer.com/tlsca/tlsca.retailer.com-cert.pem"

  # Copy retailer's CA cert to retailer's /ca directory (for use by clients)
  mkdir -p "${PWD}/organizations/peerOrganizations/retailer.com/ca"
  cp "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem" "${PWD}/organizations/peerOrganizations/retailer.com/ca/ca.retailer.com-cert.pem"

  infoln "Registering peer0"
  set -x
  fabric-ca-client register --caname ca-retailer --id.name peer0 --id.secret peer0pw --id.type peer --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering user"
  set -x
  fabric-ca-client register --caname ca-retailer --id.name user1 --id.secret user1pw --id.type client --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the org admin"
  set -x
  fabric-ca-client register --caname ca-retailer --id.name retaileradmin --id.secret retaileradminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the peer0 msp"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:2054 --caname ca-retailer -M "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/msp" --csr.hosts peer0.retailer.com --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/retailer.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/msp/config.yaml"

  infoln "Generating the peer0-tls certificates"
  set -x
  fabric-ca-client enroll -u https://peer0:peer0pw@localhost:2054 --caname ca-retailer -M "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/tls" --enrollment.profile tls --csr.hosts peer0.retailer.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the peer's tls directory that are referenced by peer startup config
  cp "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/tls/tlscacerts/"* "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/tls/ca.crt"
  cp "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/tls/signcerts/"* "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/tls/server.crt"
  cp "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/tls/keystore/"* "${PWD}/organizations/peerOrganizations/retailer.com/peers/peer0.retailer.com/tls/server.key"

  infoln "Generating the user msp"
  set -x
  fabric-ca-client enroll -u https://user1:user1pw@localhost:2054 --caname ca-retailer -M "${PWD}/organizations/peerOrganizations/retailer.com/users/User1@retailer.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/retailer.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/retailer.com/users/User1@retailer.com/msp/config.yaml"

  infoln "Generating the org admin msp"
  set -x
  fabric-ca-client enroll -u https://retaileradmin:retaileradminpw@localhost:2054 --caname ca-retailer -M "${PWD}/organizations/peerOrganizations/retailer.com/users/Admin@retailer.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/retailer/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/peerOrganizations/retailer.com/msp/config.yaml" "${PWD}/organizations/peerOrganizations/retailer.com/users/Admin@retailer.com/msp/config.yaml"
}

function createOrderer() {
  infoln "Enrolling the CA admin"
  mkdir -p organizations/ordererOrganizations/example.com

  export FABRIC_CA_CLIENT_HOME=${PWD}/organizations/ordererOrganizations/example.com

  set -x
  fabric-ca-client enroll -u https://admin:adminpw@localhost:9054 --caname ca-orderer --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  echo 'NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: cacerts/localhost-9054-ca-orderer.pem
    OrganizationalUnitIdentifier: orderer' > "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml"

  # Since the CA serves as both the organization CA and TLS CA, copy the org's root cert that was generated by CA startup into the org level ca and tlsca directories

  # Copy orderer org's CA cert to orderer org's /msp/tlscacerts directory (for use in the channel MSP definition)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/msp/tlscacerts"
  cp "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem" "${PWD}/organizations/ordererOrganizations/example.com/msp/tlscacerts/tlsca.example.com-cert.pem"

  # Copy orderer org's CA cert to orderer org's /tlsca directory (for use by clients)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/tlsca"
  cp "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem" "${PWD}/organizations/ordererOrganizations/example.com/tlsca/tlsca.example.com-cert.pem"

  infoln "Registering orderer"
  set -x
  fabric-ca-client register --caname ca-orderer --id.name orderer --id.secret ordererpw --id.type orderer --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Registering the orderer admin"
  set -x
  fabric-ca-client register --caname ca-orderer --id.name ordererAdmin --id.secret ordererAdminpw --id.type admin --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  infoln "Generating the orderer msp"
  set -x
  fabric-ca-client enroll -u https://orderer:ordererpw@localhost:9054 --caname ca-orderer -M "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp" --csr.hosts orderer.example.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml" "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp/config.yaml"

  infoln "Generating the orderer-tls certificates"
  set -x
  fabric-ca-client enroll -u https://orderer:ordererpw@localhost:9054 --caname ca-orderer -M "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls" --enrollment.profile tls --csr.hosts orderer.example.com --csr.hosts localhost --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  # Copy the tls CA cert, server cert, server keystore to well known file names in the orderer's tls directory that are referenced by orderer startup config
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/tlscacerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/ca.crt"
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/signcerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/server.crt"
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/keystore/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/server.key"

  # Copy orderer org's CA cert to orderer's /msp/tlscacerts directory (for use in the orderer MSP definition)
  mkdir -p "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp/tlscacerts"
  cp "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls/tlscacerts/"* "${PWD}/organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp/tlscacerts/tlsca.example.com-cert.pem"

  infoln "Generating the admin msp"
  set -x
  fabric-ca-client enroll -u https://ordererAdmin:ordererAdminpw@localhost:9054 --caname ca-orderer -M "${PWD}/organizations/ordererOrganizations/example.com/users/Admin@example.com/msp" --tls.certfiles "${PWD}/organizations/fabric-ca/ordererOrg/ca-cert.pem"
  { set +x; } 2>/dev/null

  cp "${PWD}/organizations/ordererOrganizations/example.com/msp/config.yaml" "${PWD}/organizations/ordererOrganizations/example.com/users/Admin@example.com/msp/config.yaml"
}
